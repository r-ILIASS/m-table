import styled from "styled-components";

export const Wrapper = styled.div`
  padding: 3px;
  align-items: center;
  justify-content: center;
  gap: 9px !important;

  img {
    max-width: 26px;
    border-radius: 50%;
  }

  p {
    font-size: 0.9rem;
    color: var(--nav);
  }
`;
